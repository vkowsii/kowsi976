# include<stdio.h>
int  main()
{ 
int  base;
int  exponent;
print( "enter the value");
scanf( "%d",&base);
print( "entet the value");
scanf( "%d",&exponent);
while( exponent !=1)
{ 
result= base;
+++exponent;
} 
printf( "the answer is);
} 
} 
